- if user_signed_in?
  .BtnAndCount
    .Like_Btn
      - if current_user.already_liked?(@post)
        = link_to post_like_path(@post), method: :delete, class: 'LikeBtn' do
          = icon('fas', 'heart')
      - else
        = link_to post_likes_path(@post), method: :post, class: 'UnLikeBtn' do
          = icon('far', 'heart')
    .LikeCount 
      = @post.likes.count
  -# .LikedUser
  -#   いいねしたユーザー：
  -#   - @post.liked_users.each do |user|
  -#     = @post.user.name