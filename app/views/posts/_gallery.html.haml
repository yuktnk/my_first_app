.Gallery
  .Gallery__title - Photo Gallery -
  %ul.Gallery__lists
    - @posts.shuffle.each do |post|
      %li.item
        = image_tag post.image.url, class: 'GalleryPicture huwahuwa inner'
        = link_to post_path(post.id) do
          .caption
            .captionTitle
              %span Title：
              = post.title
            .captionBy
              %span by：
              = post.user.name 
              %span さん
            .captionLike
              %span いいね数：
              = post.likes.count
            .captionComment
              %p Comment：
              = post.comment
-# .start
-#   %p
-#     = image_tag src="logo.png"
